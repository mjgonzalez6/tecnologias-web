<%= form_for(management) do |f| %>
  <% if management.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(management.errors.count, "error") %> prohibited this management from being saved:</h2>

      <ul>
      <% management.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %>
    <%= f.select :person_id, options_for_select(Person.all.map{|s|[s.to_s, s.id]}) %>
  </div>

  <div class="field">
    <%= f.label :document_id %>
    <%= f.select :document_id, options_for_select(Document.all.map{|s|[s.to_s, s.id]}) %>
  </div>

  <div class="actions">
    <%= f.button :submit => "", class: "btn btn-success"%>
  </div>
<% end %>

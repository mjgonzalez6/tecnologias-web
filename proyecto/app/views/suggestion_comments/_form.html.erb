<%= form_for(suggestion_comment) do |f| %>
  <% if suggestion_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suggestion_comment.errors.count, "error") %> prohibited this suggestion_comment from being saved:</h2>

      <ul>
      <% suggestion_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :suggestion_id %>
    <%= f.text_field :suggestion_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :text %>
    <%= f.text_area :text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
